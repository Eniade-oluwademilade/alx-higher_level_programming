#!/usr/bin/pytyon3

"""Function divides all elements of matrix"""
def matrix_divided(matrix, div):
    """matrix must be a list of integers or floats.
    Raises TypeError if:
       input is not a matrix of ints or floats.
       each row of matrix is of different sizes
    Raises ZeroDivisionError if:
    div is not a number or <= 0
    Returns: new matrix
    """
